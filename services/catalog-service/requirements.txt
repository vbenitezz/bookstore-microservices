# =============================================================================
# Catalog Service — Dependencias Python
# =============================================================================

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Base de datos
sqlalchemy==2.0.30
asyncpg==0.29.0          # Driver PostgreSQL async
alembic==1.13.1          # Migraciones

# Validación (incluido en FastAPI, pero explícito)
pydantic==2.7.1
pydantic-settings==2.2.1

# gRPC
grpcio==1.64.0
grpcio-tools==1.64.0     # Para compilar .proto

# Logging
loguru==0.7.2

# Utilidades
python-dotenv==1.0.1
psycopg2-binary==2.9.9   # Driver sync (usado por Alembic)

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0            # Cliente HTTP para tests de FastAPI